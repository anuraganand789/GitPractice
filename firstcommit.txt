first file to test for git-add and git -commit commands
using git status -s - for short status
let us run git diff command
added a line - for demonstrating that a file can be in staged and modified stages
added a line - for demonstrating that a file can be in staged and modified stages - this is the second line. And firstcommit.txt should be in both staged and modified stages
